import { PCAExerciseModel } from '@shared/models/exercises/PCAExerciseModel';
import { AbstractExerciseMapper } from './AbstractExerciseMapper';

/**
 * Class PCAExerciseMapper
 * Cette classe transforme les données de modèle à json stringifié ou de json stringifié à modèle
 */
export class PCAExerciseMapper extends AbstractExerciseMapper<PCAExerciseModel> {}
